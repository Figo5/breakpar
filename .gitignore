# dependencies
node_modules

# next.js
.next
out

# production / tooling
.vercel
coverage
*.tsbuildinfo
next-env.d.ts

# env files (keep .env.example tracked)
.env
.env.*
!.env.example

# clerk configuration (can include secrets)
/.clerk/

# OS
.DS_Store

# ad-hoc review screenshots (not build artifacts, not source)
/review-screenshots/

# weekly tournament field-mean log (prod data, user-local)
field-means.jsonl
